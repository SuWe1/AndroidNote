<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ysw">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".rxjavaretrofit.map.MapActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--berisaier -->
        <activity android:name=".berisaier.activity.Act1"/>
        <activity android:name=".berisaier.activity.Act2"/>
        <activity android:name=".berisaier.activity.Act3"/>
        <activity android:name=".berisaier.activity.MainActivity"/>
    </application>

</manifest>
